# AccessMemory 路忆：MVP 产品需求文档

## 文档信息

| 字段 | 内容 |
| --- | --- |
| 产品名称 | AccessMemory 路忆 |
| 文档版本 | 1.0 |
| 文档状态 | Demo Baseline |
| 目标版本 | Insta360 黑客松公益赛道 MVP |
| 最后更新 | 2026-09-23 |
| 目标平台 | 桌面 Web，兼容移动端查看 |
| 需求负责人 | AccessMemory 项目组 |

本文档是 AccessMemory MVP 的需求、范围和验收依据。除非特别标记为“规划中”，文中 P0 能力应能在当前 Demo 中完成可交互展示。

## 1. 产品概述

AccessMemory 是面向轮椅使用者、低视力与全盲用户，以及其他行动不便人群的 AI 动态无障碍三维地图。

产品需要回答三个核心问题：

1. 目的地及周边真实空间是什么样？
2. 这条路是否符合当前用户的通行条件？
3. 当路况变化后，用户应该改走哪条路线？

产品使用 Insta360 X5 采集真实空间，经 AHOLO 生成 Gaussian Splatting 三维场景；地图业务层维护地点、道路、无障碍设施、风险和状态数据，并根据用户通行档案规划可达路线。GO Ultra、手机影像和用户主动上报可作为动态路况的数据来源。

### 1.1 核心闭环

```text
真实空间重建
  -> 无障碍设施与道路建模
  -> 个性化路线规划
  -> 第一视角导航
  -> 发现新障碍
  -> 更新道路状态
  -> 自动重新规划
```

### 1.2 产品原则

- **真实可达优先**：不以几何距离替代实际通行能力。
- **个人条件优先**：不同用户可以得到不同路线和提示。
- **状态透明**：明确区分可通行、谨慎、阻断和未知。
- **避免过度承诺**：低置信度结果必须标记为待确认。
- **视觉与语音并行**：关键信息不能只依赖颜色表达。

## 2. 背景与问题

普通地图通常只表达道路连通和距离，缺少坡道、台阶、门槛、通道宽度、无障碍入口及临时占道等信息。静态“无障碍设施”标签也不能说明设施当前是否可用。

这会导致以下问题：

- 用户到达现场后才发现入口有台阶或坡道被占用。
- 地理距离最近的设施并非真正可达。
- 导航在复杂岔路或建筑入口处缺少“最后一段”细节。
- 施工、车辆和临时围挡发生后，旧路线仍被继续推荐。
- 管理者缺少带位置、时间和现场证据的无障碍风险记录。

## 3. 产品目标

### 3.1 MVP 目标

| ID | 目标 | 成功标准 |
| --- | --- | --- |
| G-01 | 展示真实空间中的无障碍细节 | 四个独立三维场景可切换，地点和设施与当前场景对应 |
| G-02 | 生成符合个人通行条件的路线 | 路由能够避开阻断道路及用户禁止的台阶或陡坡 |
| G-03 | 提供可演示的导航过程 | 从路线起点进入第一视角，并逐节点连续到达终点 |
| G-04 | 展示动态更新闭环 | 障碍分析后道路状态变化，原路线失效并生成替代路线 |
| G-05 | 覆盖多类无障碍用户 | 轮椅、低视力、全盲档案均可选择并影响路线或提示 |
| G-06 | 支持比赛现场稳定演示 | 核心闭环无需远程服务即可在本地完成 |

### 3.2 非目标

MVP 不承诺以下能力：

- 真实城市级道路覆盖与实时交通数据。
- 医疗级或安全关键级导航保证。
- 账号、组织、权限和多设备同步。
- 后台数据库持久化及审核工作流。
- 真实 GPS / UWB 室内定位。
- 生产级多模态模型准确率和自动发布机制。
- 相机固件级自定义按键联动。

## 4. 目标用户

### 4.1 主要用户

| 用户 | 核心需求 | 关键信息 |
| --- | --- | --- |
| 轮椅及行动障碍用户 | 避开台阶、陡坡、窄路和阻断入口 | 坡道、门槛、宽度、电梯、无障碍入口 |
| 低视力用户 | 通过高对比视觉和语音获得明确方向 | 当前路线、方向、路口结构、风险提醒 |
| 全盲用户 | 使用语音理解路线和环境变化 | 分段指令、距离、地标、障碍类型 |

### 4.2 次要用户

| 用户 | 核心需求 |
| --- | --- |
| 陪同人员 | 出发前查看真实环境并规划可达路线 |
| 园区管理者 | 汇总无障碍风险、证据和发现时间，用于巡检整改 |
| 志愿者或社区成员 | 上报道路变化，帮助后续用户获取更新信息 |

## 5. MVP 范围与优先级

优先级定义：

- **P0**：比赛 Demo 必须可用，缺失会破坏核心闭环。
- **P1**：增强完整性，允许使用本地数据或 Mock。
- **P2**：生产化方向，不纳入当前版本验收。

| 模块 | P0 | P1 | P2 |
| --- | --- | --- | --- |
| 三维地图 | 四场景、POI、快捷观察、第一视角、天空环境 | 更多设施标注 | 城市级场景管理 |
| 路线导航 | 通行档案、偏好、可达路由、分步导航、语音 | 收藏与熟悉路线 | 实时定位和偏航检测 |
| 动态路况 | 状态更新、阻断、自动改道 | 用户上报、影像分析 Mock | 真实 VLM 与审核发布 |
| 设施查询 | 可达设施列表与定位 | 更多设施类型 | 跨场景搜索与预约 |
| 巡检 | 风险列表、摘要导出 | 证据关联 | 整改工单系统 |
| 无障碍体验 | 关怀模式、状态文字、语音 | 移动端优化 | 完整 WCAG 审计 |

## 6. 核心用户流程

### 6.1 出发前规划

1. 用户进入指定园区场景。
2. 用户查看 3D 空间、地点标签和无障碍设施。
3. 用户选择通行档案并设置路线偏好。
4. 用户选择起点与终点并规划路线。
5. 系统展示距离、预计时间、路线标签、分段指令和最后一段提示。

### 6.2 第一视角导航

1. 用户点击“开始导航”。
2. 系统切换到 3D 第一视角并定位到规划路线起点。
3. 用户点击“下一节点”，相机沿路线连续移动到下一个坐标。
4. 系统更新当前节点、下一段距离和语音提示。
5. 到达终点后，系统显示完成状态和最后一段说明。

### 6.3 动态改道

1. 用户打开影像分析并选择或使用预置出行影像。
2. 系统展示分阶段分析进度和障碍判断。
3. 对应道路状态更新为 `BLOCKED`。
4. 地图、风险列表和二维路网同步更新。
5. 系统重新计算路线并说明已避开的风险路段。

## 7. 功能需求

### 7.1 三维场景与地图

| ID | 优先级 | 需求 | 验收标准 | MVP 状态 |
| --- | --- | --- | --- | --- |
| MAP-001 | P0 | 系统必须提供四个独立场景 | 可在入口园区、酒店连廊、酒店内庭、湖畔园林间切换；每次只加载一个模型 | 已实现 |
| MAP-002 | P0 | 场景必须支持总览和细节观察 | 鼠标可旋转、缩放视角；数字键 `1` 至 `4` 可连续定位观察点；`R` 返回总览 | 已实现 |
| MAP-003 | P0 | 场景必须显示地点与设施标签 | 标签使用当前场景数据，并可定位到对应三维坐标 | 已实现 |
| MAP-004 | P0 | 系统必须提供 2D / 3D 双视图 | 两种视图共享道路状态和当前规划路线，切换不修改业务数据 | 已实现 |
| MAP-005 | P0 | 三维背景必须具有完整环境 | 模型之外显示蓝天与云层，不出现大面积黑色背景 | 已实现 |
| MAP-006 | P1 | 清晰室外道路支持自由第一视角 | 入口和湖畔场景可通过按钮或 `M` 进入；支持键鼠移动和地点快速前往 | 已实现 |

### 7.2 通行档案与路线规划

| ID | 优先级 | 需求 | 验收标准 | MVP 状态 |
| --- | --- | --- | --- | --- |
| ROUTE-001 | P0 | 用户必须能够选择起点和终点 | 选择项来自当前场景可导航地点；修改后路线自动更新 | 已实现 |
| ROUTE-002 | P0 | 系统必须提供轮椅、低视力、全盲档案 | 三类档案可切换；路线权重、禁止条件或时间估算至少一项发生对应变化 | 已实现 |
| ROUTE-003 | P0 | 用户必须能够设置基础路线偏好 | 支持避开台阶、避开陡坡、优先平稳宽路 | 已实现 |
| ROUTE-004 | P0 | 路由必须遵守道路状态和禁止条件 | `BLOCKED` 路段不得进入路线；轮椅或“避开台阶”启用时不得经过台阶路段 | 已实现 |
| ROUTE-005 | P0 | 路线结果必须可理解 | 展示总距离、预计时间、状态标签、分段指令和最后一段提示 | 已实现 |
| ROUTE-006 | P0 | 无可达路线时必须明确反馈 | 不展示无效结果，并提示当前条件下没有可达路线 | 已实现 |
| ROUTE-007 | P1 | 设施排序必须考虑可达性 | 设施列表展示从当前起点计算的路线距离，不可达设施必须明确标识 | 已实现 |

### 7.3 第一视角引导导航

| ID | 优先级 | 需求 | 验收标准 | MVP 状态 |
| --- | --- | --- | --- | --- |
| NAV-001 | P0 | 导航必须从规划路线起点开始 | 点击“开始导航”后切换至第一视角，相机坐标对应 `route.nodes[0]` | 已实现 |
| NAV-002 | P0 | 用户必须能逐节点推进 | “下一节点”每次只前往路线中的下一个坐标，移动过程连续，完成前按钮不可重复触发 | 已实现 |
| NAV-003 | P0 | 导航状态必须同步更新 | 到达后更新当前地点、节点进度、下一目的地和路线进度条 | 已实现 |
| NAV-004 | P0 | 到达终点必须有完成状态 | 按钮显示“已到达终点”，停止继续前进，并展示最后一段提示 | 已实现 |
| NAV-005 | P0 | 导航必须支持语音提示 | 开始、到达中间节点和到达终点时调用浏览器中文语音播报 | 已实现 |
| NAV-006 | P1 | 用户可使用键盘辅助探索 | `W/S` 前后、`A/D` 横向微调、`Shift` 加速；鼠标向左时视角向左转 | 已实现 |

### 7.4 动态路况与 AI 分析

| ID | 优先级 | 需求 | 验收标准 | MVP 状态 |
| --- | --- | --- | --- | --- |
| DYNAMIC-001 | P0 | 道路必须具有统一状态 | 支持 `OPEN`、`CAUTION`、`BLOCKED`、`UNKNOWN`，并同时使用颜色与文字表达 | 已实现 |
| DYNAMIC-002 | P0 | 影像分析必须有稳定演示流程 | 可选择图片或视频；展示阶段进度；最终返回障碍原因、位置、置信度和时间 | Mock 已实现 |
| DYNAMIC-003 | P0 | 分析结果必须更新对应道路 | 分析完成后目标路段变为 `BLOCKED`，2D、3D 和风险数据同步变化 | 已实现 |
| DYNAMIC-004 | P0 | 道路阻断后必须自动改道 | 新路线不得包含阻断路段，并展示新距离及改道提示 | 已实现 |
| DYNAMIC-005 | P2 | 系统应融合多源证据和置信度 | 真实 VLM、历史记录和用户上报经过审核或加权后再发布 | 规划中 |

### 7.5 上报、设施与巡检

| ID | 优先级 | 需求 | 验收标准 | MVP 状态 |
| --- | --- | --- | --- | --- |
| REPORT-001 | P1 | 用户能够主动上报道路状态 | 可选择路段、通行状态，填写说明并选择媒体文件 | 已实现 |
| REPORT-002 | P1 | 上报后必须更新当前会话地图 | 路段、路线和风险列表立即更新；刷新页面后允许恢复 Demo 初始数据 | 已实现 |
| FACILITY-001 | P1 | 用户能够查看当前场景设施 | 列表展示设施名称、状态、说明及从当前起点计算的可达距离 | 已实现 |
| FACILITY-002 | P1 | 设施可以在三维场景中定位 | 点击“定位”后相机连续移动到对应地点 | 已实现 |
| INSPECT-001 | P1 | 系统必须汇总无障碍风险 | 展示风险等级、位置、问题、时间和数据来源 | 已实现 |
| INSPECT-002 | P1 | 系统能够导出巡检摘要 | 浏览器下载包含当前风险项的 UTF-8 文本文件 | 已实现 |

### 7.6 无障碍交互与视觉

| ID | 优先级 | 需求 | 验收标准 | MVP 状态 |
| --- | --- | --- | --- | --- |
| ACCESS-001 | P0 | 关键信息不得仅依赖颜色 | 道路状态、风险等级和导航阶段具有可读文字或图标 | 已实现 |
| ACCESS-002 | P0 | 系统必须提供关怀模式 | 启用后增大关键文字与交互区域，并提高前景和背景对比度 | 已实现 |
| ACCESS-003 | P0 | 页面必须适配桌面和移动视口 | 在 `1440x900`、`1200x750` 和 `390x844` 下主要控件不重叠、不超出视口 | 已验证 |
| ACCESS-004 | P0 | 地图必须保持产品级视觉层级 | 地图为主视觉；路线面板、工具栏、场景切换和导航条层级清晰 | 已实现 |
| ACCESS-005 | P1 | 语音开关必须可控 | 用户可以开启或关闭语音；关闭后立即停止当前播报 | 已实现 |

## 8. 数据模型与业务规则

### 8.1 核心实体

| 实体 | 关键字段 |
| --- | --- |
| Scene | `id`、模型文件、总览机位、地点、道路、设施、风险、探索路径 |
| Node | `id`、名称、短名称、三维坐标、类型、是否可选、最后一段说明 |
| Edge | `id`、起点、终点、长度、状态、标签、更新时间、来源、原因 |
| Profile | 速度、禁止标签、谨慎系数、坡度系数、复杂度系数 |
| Route | 节点序列、道路序列、距离、时间、标签、分段指令、最后一段说明 |
| Facility | 关联地点、设施类型、状态、说明 |
| Risk | 关联道路、等级、位置、问题、时间、来源 |

### 8.2 道路状态规则

| 状态 | 定义 | 规划规则 |
| --- | --- | --- |
| `OPEN` | 最近数据确认可通行 | 正常参与计算 |
| `CAUTION` | 可通行但存在风险 | 根据用户档案增加成本 |
| `BLOCKED` | 当前无法通行 | 从路线图中排除 |
| `UNKNOWN` | 缺少足够证据 | 增加成本，并向用户标记待确认 |

### 8.3 路由规则

1. 路由使用场景道路图进行双向 Dijkstra 搜索。
2. 禁止条件优先于距离权重。
3. `BLOCKED` 永久排除于本次计算。
4. 用户启用“避开台阶”或档案禁止台阶时，带 `steps` 标签的道路必须排除。
5. 谨慎、未知、狭窄、陡坡、复杂和粗糙道路根据档案与偏好增加成本。
6. 路线展示距离使用道路业务长度，相机动画使用三维坐标间距，两者不要求数值一致。

### 8.4 MVP 数据持久化

当前场景状态保存在页面内存中。路况分析、主动上报和自动改道仅影响当前浏览器会话；刷新或切换场景页面后恢复初始数据。

## 9. 非功能需求

| ID | 类别 | 要求 |
| --- | --- | --- |
| NFR-001 | 兼容性 | 支持最新两个主要版本的 Chrome 和 Edge，浏览器需支持 WebGL 2 与 ES Modules |
| NFR-002 | 性能 | 相机转场保持连续；第一视角自动移动期间不允许重复动画竞争；高斯模型采用适合场景的 splat scale |
| NFR-003 | 稳定性 | 核心 Demo 不依赖远程 AI 服务；分析失败不得破坏原道路图和当前路线 |
| NFR-004 | 可访问性 | 交互具有键盘焦点样式；关键信息有文字；语音为增强能力而非唯一输出 |
| NFR-005 | 隐私 | MVP 不上传用户所选媒体；接入真实服务前必须获得授权并定义保留和删除机制 |
| NFR-006 | 可维护性 | 场景数据、路由、影像分析、第一视角和三维覆盖层保持模块分离 |
| NFR-007 | 可演示性 | 从进入场景到完成“分析障碍并自动改道”的完整演示应能在 5 分钟内完成 |

## 10. MVP 验收场景

### AC-01：默认无障碍路线

1. 打开入口园区。
2. 选择轮椅档案，起点为园区入口，终点为主楼入口。
3. 规划路线。

预期结果：路线长度为 `400 m`，包含入口、落客平台、景观步道、中央岔路和主楼入口，不经过入口台阶。

### AC-02：逐节点第一视角导航

1. 在 AC-01 路线下点击“开始导航”。
2. 连续点击“下一节点”。

预期结果：相机先位于园区入口，随后依次到达路线节点；每次只前进一个节点；最终显示“已到达终点”。

### AC-03：影像分析与自动改道

1. 打开影像分析并运行预置分析。
2. 等待主楼正门通道更新为阻断。

预期结果：路线从 `400 m` 更新为 `520 m`，新路线经过休憩平台、缓坡入口和侧入口，不再经过阻断道路。

### AC-04：多视图同步

1. 在完成 AC-03 后切换至 2D 路网。
2. 返回 3D 场景并打开巡检。

预期结果：2D 路线、3D 路线和风险列表均反映同一阻断状态和替代路线。

### AC-05：响应式与关怀模式

1. 在桌面和 `390x844` 移动视口打开页面。
2. 切换关怀模式和四个场景。

预期结果：关键控件不重叠；文字不溢出；场景、视图、路线和工具入口仍可操作。

## 11. 依赖与约束

- 四个 `.splat` 场景是 Demo 的必要输入，必须通过 Git LFS 获取。
- 模型加载和渲染依赖客户端 GPU 与 WebGL 2。
- Web Speech 的声音、可用性和授权由浏览器与操作系统决定。
- 当前场景中的地点、距离和设施状态为产品演示数据，不能作为真实出行安全结论。
- 影像分析结果为确定性 Mock，用于验证业务闭环，不代表模型识别准确率。

## 12. 风险与缓解措施

| 风险 | 影响 | MVP 缓解措施 |
| --- | --- | --- |
| 高斯模型体积较大 | 首次加载时间较长 | 四场景独立加载；使用 Git LFS；提供加载状态 |
| 近地模型密度不足 | 第一视角局部模糊 | 自由探索仅开放清晰道路；提供快捷观察点和引导导航 |
| AI 误识别 | 错误阻断或错误推荐 | Mock 保证演示稳定；生产版引入置信度、复核和多源融合 |
| 仅颜色表达状态 | 低视力用户难以理解 | 同时使用文字、形状、图标和语音 |
| 状态没有持久化 | 无法跨会话复用 | MVP 明确限制；后续接入数据库和审核 API |

## 13. 后续版本

### P1：Demo 增强

- 真实媒体帧预览和证据关联。
- 更精细的轮椅宽度、坡度耐受度和路面偏好。
- 收藏常用地点和熟悉路线。
- 更完整的 WCAG 键盘与读屏测试。
- 结构化 PDF / JSON 巡检报告。

### P2：生产化

- 接入真实多模态模型和道路状态审核服务。
- 云端地图、用户、设备和证据数据库。
- 手机与 GO Ultra 自动同步和定位。
- 实时偏航检测、夹角语音提示和室内定位。
- 管理端整改工单、状态回访和发布权限。
- 数据授权、脱敏、保留周期和删除机制。

## 14. 完成定义

MVP 只有在以下条件全部满足时可视为完成：

- P0 需求均可在本地环境演示。
- AC-01 至 AC-05 通过。
- 展示页和 3D 地图均能按 README 启动。
- JavaScript 业务模块通过语法检查，展示页通过生产构建。
- 仓库不包含密钥、依赖目录或未由 Git LFS 管理的大型场景文件。
- README、产品需求、已实现能力和当前限制保持一致。
