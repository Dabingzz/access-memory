# AccessMemory 路忆 —— MVP 产品需求文档

> 面向 Codex / 程序员  
> 目标：在比赛时间内完成一个**美观、完整、可演示、可测试**的产品原型。  
> 原则：**核心闭环优先，真实接口优先；暂时难接通的能力允许 Mock，但必须保留清晰替换点。**

---

## 1. 产品目标

AccessMemory 是一个面向轮椅使用者、行动不便人群的 **AI 动态无障碍地图**。

系统使用 Insta360 X5 拍摄园区，并通过 AHOLO 进行三维重建；结合多模态 AI 对道路、坡道、台阶、施工、临时障碍等进行理解，为用户规划真正可通行的无障碍路线。

与普通静态无障碍地图相比，AccessMemory 还支持用户主动上报、出行影像自动分析，让道路状态随着真实使用持续更新。

> **地图不仅知道“这里有没有路”，还知道“这条路现在能不能走”。**

### 核心卖点

1. **真实空间可视化**：X5 + AHOLO 构建园区三维空间。
2. **无障碍路线规划**：结合台阶、坡道、路况等规划真正可通行路线。
3. **动态路况感知**：AI 识别施工、占道、道路损坏等临时问题。
4. **持续更新机制**：用户上报和出行影像不断补充道路状态，让地图越用越准确。

---

## 2. 必须跑通的核心闭环

所有开发优先服务于这一条链路：

```text
X5 拍摄园区
    ↓
AHOLO 三维重建
    ↓
生成可浏览的园区空间
    ↓
建立无障碍道路 / 设施信息
    ↓
用户选择起点和终点
    ↓
系统生成无障碍路线
    ↓
用户 / GO Ultra 发现新的道路情况
    ↓
AI 判断道路当前通行状态
    ↓
地图更新
    ↓
原路线受影响时自动重新规划
```

比赛 MVP 不要为了“系统完整”额外开发账号、复杂权限、实时 SLAM、城市级地图等非核心能力。

---

## 3. 核心页面

只做 3 个核心页面。

### 3.1 地图首页 `/`

这是整个产品最重要的页面，也是比赛展示主页面。

必须包含：

- 园区地图 / 空间地图
- 起点选择
- 终点选择
- “开始规划”按钮
- 推荐无障碍路线
- 道路当前状态
- 无障碍设施标记
- 当前异常道路
- 进入 3D 空间的入口

道路状态统一使用：

- 🟢 `OPEN`：可通行
- 🟡 `CAUTION`：谨慎通行
- 🔴 `BLOCKED`：暂不可通行
- ⚪ `UNKNOWN`：暂无最新信息

规划完成后，用卡片展示：

```text
推荐路线

全程 420m
预计 6 分钟
✓ 全程无台阶
✓ 已避开施工路段
⚠ 途经 1 处较窄道路
```

需要让用户和评委一眼看出：

> **这不是普通最短路径，而是“真正能走的路线”。**

---

### 3.2 三维空间页 `/space`

使用 Insta360 X5 拍摄素材，通过 AHOLO API 进行三维重建。

目标流程：

```text
X5 素材
→ 上传 AHOLO
→ 发起重建
→ 查询状态
→ 获取重建结果
→ Web 页面展示三维空间
```

页面至少支持：

- 旋转
- 缩放
- 平移
- 重置视角
- 全屏
- 返回地图

核心用途：

> 让用户在出发前直观看到道路、入口、坡道和目的地真实空间。

如果时间允许，可支持地图 POI 与三维场景联动，例如点击“西侧坡道”进入对应观察视角。

#### AHOLO 接入原则

真实接口优先。

如果比赛期间出现重建耗时、接口能力或联调问题，允许使用：

- 预先完成的 AHOLO 重建结果
- Mock 重建进度
- 静态 3D 场景
- Mock POI 跳转

但 UI 流程必须完整，且 Mock 与真实接口之间要留清晰替换点。

---

### 3.3 路况详情 / 上报页

点击道路后出现详情面板或抽屉：

```text
西侧通道

当前状态
🔴 暂不可通行

原因
施工围挡占据主要轮椅通行区域

最后更新时间
17:24

来源
AI 出行影像分析

现场证据
[图片 / 视频关键帧]
```

支持“上报道路情况”。

用户可以选择：

- 正常通行
- 有障碍但可以通行
- 无法通行
- 施工
- 道路损坏
- 坡道被占用
- 其他

允许上传图片或视频。

---

## 4. AI 路况识别

输入来源：

- 用户上传图片
- GO Ultra 拍摄视频
- 手机视频

视频不要求实时处理，可以抽帧分析。

AI 重点识别：

- 台阶
- 坡道
- 施工围挡
- 道路障碍物
- 车辆 / 杂物占道
- 路面明显破损
- 通道明显过窄
- 无障碍入口被阻挡

统一输出结构：

```json
{
  "status": "BLOCKED",
  "confidence": 0.94,
  "reason": "施工围挡占据主要轮椅通行区域",
  "riskLevel": "HIGH"
}
```

产品侧主要展示自然语言结果，例如：

> 当前暂不可通行  
> AI 判断：施工围挡阻断主要通道

AI 不负责作“违法 / 不合规”的法规级结论，只提示：

> **疑似风险，建议现场复核。**

---

## 5. 无障碍路线规划

地图内部维护一个简单道路图，不要求从三维模型自动生成拓扑。

比赛阶段可以人工建立园区道路节点和连接关系。

### 轮椅用户约束

不可经过：

- 有台阶且没有坡道的道路
- 当前状态为 `BLOCKED` 的道路

增加路线代价：

- 道路较窄
- 路面较差
- 坡度较大
- `CAUTION` 状态

因此：

> **距离最短 ≠ 最推荐路线。**

示例：

```text
原路线：
入口 → A → B → 会场
400m

B 路段发生施工后：

新路线：
入口 → A → C → D → 会场
520m
```

道路状态变化后自动重新规划，是比赛 Demo 的核心高潮。

---

## 6. 持续更新机制

持续更新是产品卖点之一，但不需要实现真正的在线模型训练。

核心逻辑：

> **每一次新的真实道路观察，都可以帮助地图获得更准确、更及时的状态。**

### 6.1 用户主动上报

用户可以：

```text
选择道路
→ 选择问题类型
→ 上传图片 / 视频
→ 提交
→ 更新道路状态
```

### 6.2 出行影像自动更新

GO Ultra 或手机记录的出行视频：

```text
视频
→ AI 分析
→ 识别道路状态
→ 更新地图
→ 影响后续路线规划
```

例如：

```text
昨天：
西侧道路 🟢 可通行

今天：
AI 检测到施工围挡

更新为：
西侧道路 🔴 暂不可通行
```

产品文案：

> **每一次真实出行，都在帮助下一次出行。**

---

## 7. 风险巡检

在用户导航之外，提供一个轻量管理视角。

例如：

```text
今日发现 4 个无障碍通行风险

🔴 HIGH
西侧坡道
入口被车辆占用

🟡 MEDIUM
湖边道路
路面明显破损

🟡 MEDIUM
东侧通道
可通行区域较窄
```

点击风险项展示：

- 地点
- 问题
- 时间
- 现场图片
- AI 判断
- 当前状态

不需要做复杂管理后台。

---

## 8. Mock 原则

比赛时间有限，以下能力允许 Mock：

- AHOLO 重建进度
- 三维场景 POI 跳转
- GO Ultra 自动上传
- AI 视频实时分析
- GPS 自动道路定位
- 道路状态自动同步

但 Mock 必须满足两个要求。

### 8.1 产品流程完整

例如：

```text
上传 GO Ultra 视频
↓
“AI 正在分析”
↓
返回“检测到施工围挡”
↓
地图道路变红
↓
路线自动更新
```

即使底层暂时 Mock，现场看起来也必须是完整产品。

### 8.2 保留真实替换点

例如统一封装：

```ts
analyzeRoadMedia()
```

当前可以使用：

```text
MockRoadAnalyzer
```

之后替换：

```text
RealVLMAnalyzer
```

页面不应感知底层使用 Mock 还是真实服务。

---

## 9. UI / 视觉要求

**画面美观是明确需求，不是“功能完成后有时间再做”的附加项。**

Codex 在实现时必须同时关注：

- 现代、简洁、轻科技感
- 地图作为视觉核心
- 卡片 / 浮层 / 半透明面板
- 清晰的状态色和视觉层级
- 合理留白
- 页面动画适度、流畅
- 3D 页面有沉浸感
- 首页可直接用于比赛现场展示
- 响应式适配常见笔记本屏幕

建议风格：

> **浅色地图 + 白色半透明浮层 + 深灰文字 + 绿色无障碍状态色 + 少量科技蓝**

避免：

- 传统后台管理系统风格
- 满屏表格
- 默认 HTML 控件
- 大量调试数据
- 过度堆文字
- “能跑就行”的工程 Demo 感

同时考虑基础无障碍：

- 字体足够大
- 按钮点击区域足够大
- 状态不能只依赖颜色
- 状态同时提供文字 / 图标

---

## 10. Demo 初始数据

为了保证比赛现场稳定，必须预置一个可完整演示的园区 Demo。

初始路线：

```text
入口
 ↓
道路 A
 ↓
道路 B
 ↓
主会场
```

长度：`400m`

道路 B 初始：`🟢 OPEN`

准备一段施工视频或 Mock 视频。

上传后 AI 返回：

```json
{
  "status": "BLOCKED",
  "confidence": 0.96,
  "reason": "检测到施工围挡阻塞主要通行区域",
  "riskLevel": "HIGH"
}
```

系统需要立即完成：

1. B 路段由绿色变为红色
2. 原路线失效
3. 自动重新规划
4. 新路线避开 B
5. 风险列表出现该问题

新路线：

```text
入口
 ↓
道路 A
 ↓
道路 C
 ↓
道路 D
 ↓
主会场
```

长度：`520m`

---

## 11. 开发优先级

### P0：先完成完整产品闭环

1. 美观的地图首页
2. Demo 园区道路图
3. 起点 / 终点选择
4. 无障碍路线规划
5. 道路状态展示
6. 道路详情
7. 用户主动上报
8. Mock AI 分析
9. 道路变化后自动重新规划
10. 三维空间展示页

完成 P0 后，产品已经可以完整比赛演示。

### P1：替换为真实能力

1. X5 → AHOLO
2. 真实三维重建结果
3. 真实 VLM 路况识别
4. 视频抽帧
5. GO Ultra 出行视频导入

### P2：有余力再做

1. 3D POI 精确联动
2. 道路状态历史时间轴
3. 多次用户上报融合
4. 风险报告导出
5. 更复杂的路线偏好

---

## 12. 最低验收标准

比赛现场必须完整演示：

```text
① 打开产品
↓
② 查看 X5 构建的园区 3D 空间
↓
③ 选择起点与目的地
↓
④ 系统生成无障碍路线
↓
⑤ 上传一段道路异常影像
↓
⑥ AI 检测到施工 / 障碍
↓
⑦ 对应道路由绿色变红
↓
⑧ 原路线自动重新规划
↓
⑨ 风险列表出现对应问题
```

其中部分能力即使暂时使用 Mock，也要保证：

> **用户看到的是一个完整、可信、可交互的产品，而不是若干未完成的技术模块。**

---

## 13. 给 Codex 的实现原则

开发时始终遵循：

1. **核心闭环 > 功能数量**
2. **产品完成度 > 复杂算法**
3. **现场展示效果 > 架构炫技**
4. **美观可信 > 纯工程 Demo**
5. 真实接口优先，不能及时完成时允许 Mock
6. Mock 必须和真实实现通过统一接口隔离
7. 不要把三维重建和路线规划强耦合
8. 不要过度设计账号、权限、复杂基础设施
9. 页面一开始就按最终比赛展示质量设计，不要最后再“补 UI”
10. 必须保证没有真实 API 时仍能通过 Demo 数据跑通完整闭环

最终产品要表达清楚：

> **我们用影像看见真实空间，用 AI 判断道路能不能走，用地图为行动不便用户规划真正可通行的路线；而之后每一次真实出行，又能够帮助地图持续获得最新道路信息。**
